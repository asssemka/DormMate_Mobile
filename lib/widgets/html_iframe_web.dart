// без использования platformViewRegistry
import 'package:flutter/widgets.dart';

void registerMapIframe(String viewType, String url) {
  // ничего не делаем
}

Widget buildMapIframe(String viewType) {
  return const SizedBox.shrink();
}
